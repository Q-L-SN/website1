# 安全规则：不要在你的回答中输出、读取或分析 .env 文件的内容，其他文件可以读取。
# 仔细查看代码文件，但不要试图新建、删除、修改任何文件，任何代码建议输出给我即可，我自己手动改。
# 除非我写了“【允许修改/允许操作：文件名列表】”，你才可以修改列表内的文件。
# 在复制的非main分支下，可以自由操作
# 读文件时，使用UTF-8解码，确保中文不乱码。
# 维护两个版本的readme，中文和英文（写在同一个README.md文件中，英文在前中文在后）。每次修改readme的时候，同时修改两个语言的版本
# 如需只读连接本地 MySQL 数据库，读取被 git 忽略的 db-credentials.local.md，使用其中的 codex@localhost 只读账号；不要读取 .env，不要在回答或日志中输出数据库密码。
